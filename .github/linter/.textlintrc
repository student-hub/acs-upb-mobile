##### Configuration file for textlint, the documentation linter. #####

{
    "rules": {
        "@textlint-rule/textlint-rule-no-unmatched-pair": true,
        "@textlint-rule/textlint-rule-proselint": true,
        "textlint-rule-no-dead-link": {
            "ignoreRedirects": true,
            "retry": 10
        }
    }
}
