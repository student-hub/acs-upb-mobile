import 'question.dart';

class FeedbackQuestionSlider extends FeedbackQuestion {
  FeedbackQuestionSlider({
    String question,
    String category,
    String id,
    String answer,
  }) : super(
          question: question,
          category: category,
          id: id,
          answer: answer,
        );
}
